
const HOST = 'https://alpha.mapquest.io';

const HOSTS = {
    'et-devices': HOST,
    'et-rules': HOST,
    'et-auth': HOST
};

export const ENVIRONMENT_CONFIGURATION = {
    API_HOSTS: {
        alpha: HOSTS
    }
};

export const CURRENT_ENVIRONMENT = ENVIRONMENT_CONFIGURATION.API_HOSTS['alpha'];
