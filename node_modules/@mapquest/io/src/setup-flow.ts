import { ApplicationContext } from './application-context';
import { getConfiguration } from './';

export default function buildApplicationContext(applicationId: string, secret:string = getConfiguration().secret):ApplicationContext {
  let context = new ApplicationContext(secret, applicationId);
  return context;
};


export * from './';
