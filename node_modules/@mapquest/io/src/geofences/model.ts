import { IRule } from '../rules/';
import { IRegion } from '../regions/';
import { EnterTransition, ExitTransition, DwellTransition } from '../transitions/';

export interface IGeofence extends IRule {
  geofenceId?: string;
  authorId?: string;
  appId: string;
  clientId: string;
  region: IRegion;
  enter?: EnterTransition;
  exit?: ExitTransition;
  dwell?: DwellTransition;
  dwellOutside?: DwellTransition;
  created?: number;
  modified?: number;
  state?: string;
}
