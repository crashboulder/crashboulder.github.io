
export interface IRegion {
    type: string;
    longitude: number;
    latitude: number;
    radius: number;
}

export class CircularRegion implements IRegion {
    public readonly type = 'circle';
    constructor(public longitude: number, public latitude: number, public radius: number) { }
}