/// <reference types="es6-promise" />
import { GenericService } from '../generic/service';
import { IApplication } from './model';
export declare class ApplicationService extends GenericService<IApplication> {
    getAPIHost(): string;
    getRootPath(): string;
    list(criteria?: any): Promise<IApplication[]>;
}
