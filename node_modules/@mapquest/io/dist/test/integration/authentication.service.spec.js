"use strict";
var chai_1 = require('chai');
var _1 = require('../../src/security/');
var _2 = require('../../src/');
describe('Authentication Service Integration', function () {
    var authenticationService;
    var applicationId = 'test';
    var authenticationServiceAccessToken;
    beforeEach(function () {
        authenticationService = new _1.AuthenticationService({
            applicationId: applicationId,
        });
    });
    it('should obtain an access token', function (done) {
        var authCall = authenticationService.fetchApplicationAccessToken(_2.getConfiguration().secret);
        authCall.then(function (accessToken) {
            chai_1.expect(accessToken).to.exist;
            return done();
        });
    });
});
//# sourceMappingURL=authentication.service.spec.js.map