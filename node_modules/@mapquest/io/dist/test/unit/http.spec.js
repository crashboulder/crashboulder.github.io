"use strict";
var chai = require('chai');
var http_1 = require('../../src/http');
var SDK = require('../../package.json');
var expect = chai.expect;
chai.should();
describe('HTTP Client', function () {
    it('should contain a function to add user-agent to arbitrary headers', function (done) {
        var config = { headers: { 'arbitrary': 'header' } };
        var currentVersion = SDK['version'];
        var newConfig = http_1.Http.addUserAgentToConfigHeaders(config);
        expect(newConfig.headers['arbitrary']).to.equal('header');
        expect(newConfig.headers['User-Agent']).to.equal('MapQuest.io Node.js SDK (v' + currentVersion + ')');
        return done();
    });
});
//# sourceMappingURL=http.spec.js.map